import 'package:flutter/material.dart';

class StartGru extends StatelessWidget {
  const StartGru({super.key, required this.userIds});
  final List<String> userIds;

  @override
  Widget build(BuildContext context) {
    return const Card();
  }
}
